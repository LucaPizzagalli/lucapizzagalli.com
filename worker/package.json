{
  "name": "lp-newsletter-worker",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "list": "wrangler kv key list --binding SUBSCRIBERS",
    "export": "wrangler kv key list --binding SUBSCRIBERS | jq -r '.[].name'"
  },
  "devDependencies": {
    "wrangler": "^3.95.0"
  }
}
